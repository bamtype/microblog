<p id="notice"><%= notice %></p>

<h1>My Brother's Keepers - The Watchers</h1>
<h3>You were put here to protect us, but who protects us from you?</h3>
<table>
  <thead>
    <tr>
      <th>Username</th>
      <th>Fname</th>
      <th>Lname</th>
      <th>Password</th>
      <th>Email</th>
      <th colspan="5"></th>
    </tr>
  </thead>

  <tbody>
    <% @watchers.each do |watcher| %>
      <tr>
        <td><%= watcher.username %></td>
        <td><%= watcher.fname %></td>
        <td><%= watcher.lname %></td>
        <td><%= watcher.password %></td>
        <td><%= watcher.email %></td>
        <td><%= link_to 'Show', watcher %></td>
        <td><%= link_to 'Edit', edit_watcher_path(watcher) %></td>
        <td><%= link_to 'Destroy', watcher, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Watcher', new_watcher_path %>
